# SpaceXAI (xAI) — get a key at https://console.x.ai
# Server-side only; never expose to the browser.
XAI_API_KEY=your_api_key_here

# Supabase Auth — Project Settings → API
# Public (safe in browser; RLS still required)
# See docs/setup-auth.md for Google OAuth + progress table setup.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Prefer publishable key (current Supabase docs). Anon key still works.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_publishable_or_anon_key
# Alias for older tutorials / existing envs:
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Server-only — never prefix with NEXT_PUBLIC_
# Not required for user-scoped progress APIs (RLS + user JWT).
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Google Client ID/Secret are configured in the Supabase Dashboard
# (Authentication → Providers → Google), not in this app env.

