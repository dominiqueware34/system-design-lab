# System Design Lab (Claude entry)

Mandatory read order (if `@` include is not expanded, open these paths):

1. `AGENTS.md` — full agent index + Next.js rules + **feature-branch / multi-agent rules**
2. `docs/brain/PRODUCT.md`
3. `docs/brain/FEATURES.md`
4. `docs/brain/BOARD.md` — print ASCII board **before** implementation
5. `docs/brain/STATUS.md`

Before code: print the FEATURE BOARD summary from BOARD.md. One feature → one branch; parallel agents use separate worktrees.

Do not reimplement: campaign, training, auth/progress, data-flow, evaluate/wrench APIs — see FEATURES.

@AGENTS.md
